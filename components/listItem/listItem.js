function renderListItem(parent, item) {

    let con = document.createElement("li");
    con.classList.add("contactListItem");
    parent.appendChild(con);

    // profilePic
    // ListItemInfo
    // Name
    // Knownfrom

}